package main

//import "fmt"

func main() {
	/*
		var age int = 30  // int age = 30
		var name string = "Alice"
		// char *name = "Alice"  // sizeof("Alice")
		// char name[] = "Alice"
		fmt.Printf("%d, %s\n", age, name)

		var age = 30
		var pi = 3.14
		var name = "Bob"
		fmt.Printf("%d, %5.2f, %s\n", age, pi, name)

		age  := 30
		name := "Charlie"

		// int age = 30;
		// age = 40;
		fmt.Printf("%d, %s\n", age, name)
	*/
	//    var Pi1 = 3.14
	const Pi = 3.14
	const Greeting = "Hello"

	const (
		StatusOK       = 200
		StatusNotFound = 404
		StatusError    = 500
	)
}
